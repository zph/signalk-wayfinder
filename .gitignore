dist/
node_modules/
sidecar/target/
public/buildinfo.json
*.tgz
obsolete because claude did wrong/
.claude/

# GSHHG build artifacts — keep only the final compressed indices
data/gshhg-*.zip
data/_tmp_shp/
data/*.bin
!data/*.bin.gz

# Sub-package binary artifacts — built in CI, not committed
packages/*/binding/

# opencode session marker (not for the repo)
.opencode/confirmed

# Local build server info (infrastructure-specific, not for the repo)
.local-build-server.md
